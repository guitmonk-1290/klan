import Image from "next/image"

interface ChatProps {
    userId: string,
    chatId: string
}

export default async function Chat({
    userId,
    chatId
} : ChatProps) {
    return (
        <></>
    )
}